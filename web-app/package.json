{
  "name": "task-manager-web",
  "version": "1.0.0",
  "description": "Task Manager web app — course starter for GitHub Copilot for Automation Engineers",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "preview": "vite preview --port 3000",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@vitejs/plugin-react": "^6.0.3",
    "vite": "^8.1.5"
  }
}
