# Moved

Path-specific Copilot instructions now live in
`.github/instructions/typescript-tests.instructions.md` — the file-name
convention (`NAME.instructions.md` under `.github/instructions/` with an
`applyTo:` glob in the frontmatter) that GitHub Copilot actually
recognises.

This placeholder is kept only so older references don't 404 — it is safe
to delete.
