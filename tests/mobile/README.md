# Mobile Tests (Appium + WebdriverIO)

Generate here in Module 4, Lab 15: `wdio.conf.js`, `pages/TaskFormScreen.js`,
and `task-creation.spec.js`. Provide the Android resource-ids in your Copilot
prompt (Appium can't read them from source the way Playwright reads the DOM).
