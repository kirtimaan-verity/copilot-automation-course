# Cross-Layer E2E Tests

Generate here in Module 9, Lab 35: `test_task_creation_e2e.py` — a single test
that validates the UI → API → database path. You'll also add a `conftest.py`
that composes fixtures from `tests/api/conftest.py` and `tests/db/conftest.py`.
